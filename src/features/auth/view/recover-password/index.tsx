import * as React from 'react';
import { View, Text } from 'react-native';

export const RecoverPasswordView = () => (
  <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    <Text>Recover Password View</Text>
  </View>
);
